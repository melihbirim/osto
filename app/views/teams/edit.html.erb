<div data-controller="shortcuts">
  <%= link_to team_path(@team), class: "button back-button", data: { shortcut: "b" } do %>
    BACK TO TEAM (B)
  <% end %>

  <h1>EDIT TEAM</h1>

  <div class="retro-form">
    <%= form_with(model: @team, local: true) do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "retro-input" %>
      </div>

      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, class: "retro-input" %>
      </div>

      <%= f.submit "UPDATE TEAM", class: "button button-start" %>
    <% end %>
  </div>
</div>